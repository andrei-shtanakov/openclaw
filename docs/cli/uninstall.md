---
summary: "CLI reference for `orchid uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `orchid uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
orchid uninstall
orchid uninstall --all --yes
orchid uninstall --dry-run
```
