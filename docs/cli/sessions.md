---
summary: "CLI reference for `orchid sessions` (list stored sessions + usage)"
read_when:
  - You want to list stored sessions and see recent activity
title: "sessions"
---

# `orchid sessions`

List stored conversation sessions.

```bash
orchid sessions
orchid sessions --active 120
orchid sessions --json
```
